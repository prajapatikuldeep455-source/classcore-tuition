rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for ClassCore desktop & mobile companion sync
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
