{
  "version": "1.6.1",
  "notes": "main.js: removed requestHeaders (AWS S3 403 fix), removed await from downloadUpdate (unstick 0% bug). verifyForgotOTP: sends email with new credentials. Exams module: create exams, enter marks, generate report card PDFs with Send via WhatsApp/Email."
}
