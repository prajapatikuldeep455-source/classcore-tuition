{
  "version": "1.6.6",
  "notes": "Auto-update fully fixed: removed requestHeaders (403 fix), 5-min stall timeout, exponential backoff retry (3 attempts), download progress clears timeout, feedUrl from env/fallback. Loading overlay added. Receipt deletion and exam marks refresh instantly."
}
