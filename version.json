{
  "version": "1.5.7",
  "notes": "Update system: checks after 1s (was 3s), re-checks every 30 minutes, GitHub CDN cache disabled via request headers. Manual Check for Updates button shows instant feedback. Save Failed fixed: payload size guard, error only after 3 consecutive failures. Attendance v2.0: instant toggle updates."
}
